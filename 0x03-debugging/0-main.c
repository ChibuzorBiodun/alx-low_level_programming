#include "main.h"

/**
 * main - tests that the function that prints if integer is positive gives a correct output when given a case of 0
 * Return: 0
 * /
 
 int main(void)
 {
 	int i;

	i = 98;
	positive_or_negative(i);

	return (0);
}

